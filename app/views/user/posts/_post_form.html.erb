<h1 class="text-center">投稿フォーム</h1>
		<%= form_with(model: @post, url: post_path(@post), local: true) do |f| %>

		  <div class="text-center">
		  	 <div class="form-group">
		  	 	<div class="pt-5">
		  	 	   <%= f.attachment_field :image, class: 'img_field', style: 'display:none' %>
			       <%= attachment_image_tag @post, :image, :fill, 300, 300, fallback: 'm_e_others_500.png', onClick: "$('.img_field').click()", class: 'center-block img-thumbnail img-responsive img_prev' %>
			    </div>
			    <div class="pt-4">
				   <h4>趣味</h4>
				   <%= f.text_field :hobby_name, class:"form-control", placeholder:"※複数タグ付けする場合は趣味と趣味の間にスペースを入れてください"  %>
				</div>
				<div class="pt-4">
				   <h4>内容</h4>
				   <%= f.text_area :body, class:"form-control pb-5" %>
				</div>
			  </div>
			     <% if 'posts#new' %>
			      <%= f.submit "投稿", class:"btn btn-outline-success btn-lg mt-5" %>
			     <% else %>
			      <%= f.submit "編集する" %>
			     <% end %>
		  </div>
		<% end %>